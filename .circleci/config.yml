version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:14.18-bullseye

    steps:
      - checkout

      - run:
          name: "Install Dependencies"
          command: npm install

      - run:
          name: "Build"
          command: npm run build

workflows:
  version: 2
  build:
    jobs:
      - build
