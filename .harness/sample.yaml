pipeline:
  name: sample
  identifier: sample
  projectIdentifier: default_project
  orgIdentifier: default
  tags: {}
  properties:
    ci:
      codebase:
        connectorRef: Abbas_Mohammed
        build: <+input>
  stages:
    - stage:
        name: Build
        identifier: Build
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          execution:
            steps:
              - step:
                  type: Run
                  name: Run_1
                  identifier: Run_1
                  spec:
                    connectorRef: account.harnessImage
                    image: node:18-alphine
                    shell: Sh
                    command: |-
                      npm install
                      npm run build
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
